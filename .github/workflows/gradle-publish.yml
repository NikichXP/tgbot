name: Java CI with Gradle and Release

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup Java
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'

    - name: Build with Gradle
      run: ./gradlew build --info -Pversion=${{ GITHUB_RUN_NUMBER }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        VERSION: ${{ GITHUB_RUN_NUMBER }}

      with:
        tag_name: v${{ GITHUB_RUN_NUMBER }}
        release_name: Release v${{ GITHUB_RUN_NUMBER }}
        body: 'Updates'
        draft: false
        prerelease: false
